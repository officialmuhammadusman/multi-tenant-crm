{
  "version": 2,
  "buildCommand": "cd ../.. && npx pnpm@latest db:generate && npx pnpm@latest --filter @crm/api build:serverless",
  "outputDirectory": "dist",
  "functions": {
    "dist/main.serverless.js": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/dist/main.serverless.js" }
  ]
}